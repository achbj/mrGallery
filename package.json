{
  "name": "mr-gallery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "app:dev": "neu run",
    "build:web": "vite build",
    "build": "npm run test && npm run build:web",
    "app:build": "neu build --release",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@neutralinojs/lib": "^6.8.0",
    "exifr": "^7.1.3",
    "lucide": "^1.20.0"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
